import GridIntensity from './node'

// TODO if there is a way to tell which environmen we're in
// we should load in the corresponding version.
// Defaulting to node for now
export default GridIntensity
